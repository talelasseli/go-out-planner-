services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: gooutplanner
      POSTGRES_USER: gooutplanner
      POSTGRES_PASSWORD: gooutplanner
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
